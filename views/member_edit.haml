%form(action="/members/edit/#{@member.id}" method="POST")
	%input(type="hidden" name="_method" value="PUT")
	
	= haml :member_info_form, layout => false
	
	%input#submit(type="submit" value="Submit") or <a href="/members/#{@member.id}">cancel</a>