!!! 5
%html
  %head
    %meta(charset="utf-8")
    %title Members Test!
  %body
    #status
      - unless !logged_in?
        %p You are logged in as #{current_user.name} <a href="/logout">logout</a>
      - if flash.has?(:error)
        %p= flash[:error]
    = yield
